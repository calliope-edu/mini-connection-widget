diff --git a/build/cjs/bluetooth/connection.js b/build/cjs/bluetooth/connection.js
index bd3d0ed49a2062a55d257e85657ec849b7ea724f..fc985ff59697c2ceeb3e700bc659efd76ace150d 100644
--- a/build/cjs/bluetooth/connection.js
+++ b/build/cjs/bluetooth/connection.js
@@ -276,9 +276,12 @@ class MicrobitBluetoothConnectionImpl extends events_js_1.TypedEventTarget {
         // Support both the default "BBC micro:bit" name and the shorter "uBit"
         // name used by some MakeCode extensions (e.g. bsiever/microbit-pxt-blehid).
         // See https://github.com/bsiever/microbit-pxt-blehid/issues/31
+        // Patched by @calliope-edu/mini-connection-widget: include Calliope
+        // mini namePrefix so Calliope hardware shows up in the BLE picker
+        // alongside micro:bit. Mini 1/2/3 all advertise "Calliope mini [name]".
         const namePrefixes = this.nameFilter
-            ? [`BBC micro:bit [${this.nameFilter}]`, `uBit [${this.nameFilter}]`]
-            : ["BBC micro:bit", "uBit"];
+            ? [`BBC micro:bit [${this.nameFilter}]`, `uBit [${this.nameFilter}]`, `Calliope mini [${this.nameFilter}]`]
+            : ["BBC micro:bit", "uBit", "Calliope mini"];
         // If we have a cached device, check if it still matches the current filter.
         // If not, clear it so we find a new device.
         if (this.bleDevice) {
@@ -521,6 +524,11 @@ class MicrobitBluetoothConnectionImpl extends events_js_1.TypedEventTarget {
             profile_js_1.profile.magnetometer.id,
             profile_js_1.profile.temperature.id,
             profile_js_1.profile.uart.id,
+            // Patched by @calliope-edu/mini-connection-widget: declare the
+            // partial-flashing service up front so flash() can reach it on
+            // Web Bluetooth. (Capacitor's web fallback can't reach undeclared
+            // services after pairing.)
+            profile_js_1.profile.partialFlashing.id,
         ];
         // Temporarily patch navigator.bluetooth.requestDevice to support multiple
         // name prefix filters. The capacitor-ble plugin only supports a single
diff --git a/build/cjs/bluetooth/services/device-information-service.js b/build/cjs/bluetooth/services/device-information-service.js
index 8c3da78c27a79bc9759dd9af9b3079cfe245bcc6..817dc719d7736eb8a045c3e862c4a6d51fa7eade 100644
--- a/build/cjs/bluetooth/services/device-information-service.js
+++ b/build/cjs/bluetooth/services/device-information-service.js
@@ -16,13 +16,21 @@ class DeviceInformationService {
         const serviceMeta = profile_js_1.profile.deviceInformation;
         const modelNumberBytes = await bluetooth_le_1.BleClient.read(this.deviceId, serviceMeta.id, serviceMeta.characteristics.modelNumber.id);
         const modelNumber = new TextDecoder().decode(modelNumberBytes);
-        if (modelNumber.toLowerCase() === "BBC micro:bit".toLowerCase()) {
+        const lower = modelNumber.toLowerCase();
+        if (lower === "BBC micro:bit".toLowerCase()) {
             return "V1";
         }
-        if (modelNumber.toLowerCase().includes("BBC micro:bit v2".toLowerCase())) {
+        // Patched by @calliope-edu/mini-connection-widget: recognise Calliope
+        // mini hardware (mini 1/2 → V1-class DAL, mini 3 → V2-class CODAL)
+        // and tolerate firmware-revision suffixes that aren't strict matches.
+        if (lower.includes("calliope mini") || lower.includes("v2")) {
             return "V2";
         }
-        throw new Error(`Unexpected model number: ${modelNumber}`);
+        // Unknown model number — log + default to V2 instead of throwing.
+        // The only consumer of board version on the BLE path picks between
+        // V1 and V2 capability sets; defaulting to V2 is the safe bet.
+        console.warn(`[microbit-connection] Unrecognised model number "${modelNumber}", assuming V2`);
+        return "V2";
     }
 }
 exports.DeviceInformationService = DeviceInformationService;
diff --git a/build/cjs/usb/connection.d.ts b/build/cjs/usb/connection.d.ts
index 5d63b9ce50c3c80907ff33444c2898f118379e32..081a4e871a7e056b6d3e75760e5f4585ae7ab553 100644
--- a/build/cjs/usb/connection.d.ts
+++ b/build/cjs/usb/connection.d.ts
@@ -58,7 +58,7 @@ export interface MicrobitUSBConnection extends DeviceConnection {
      * @returns A promise that resolves when the write is complete.
      * @throws {DeviceError} with code `not-connected` if there is no connection.
      */
-    serialWrite(data: string): Promise<void>;
+    serialWrite(data: string | Uint8Array): Promise<void>;
     /**
      * Gets micro:bit deviceId.
      *
diff --git a/build/cjs/usb/connection.js b/build/cjs/usb/connection.js
index e04b8bfd599dbe03b3d8c5adb0d63f1a780a4b59..1c98109603f77311be2bab9aa3db1b9e3afcde3b 100644
--- a/build/cjs/usb/connection.js
+++ b/build/cjs/usb/connection.js
@@ -15,7 +15,16 @@ const promise_queue_js_1 = require("./promise-queue.js");
 const device_wrapper_js_1 = require("./device-wrapper.js");
 const partial_flashing_js_1 = require("./partial-flashing.js");
 const cdc_saturation_js_1 = require("./cdc-saturation.js");
-const defaultFilters = [{ vendorId: 0x0d28, productId: 0x0204 }];
+// Patched by @calliope-edu/mini-connection-widget: include Segger J-Link
+// VID/PID pairs used by Calliope mini 1.x and 2.x boards. Without these,
+// Calliope minis using J-Link debugger firmware are invisible to the
+// WebUSB picker. Mini 3 ships DAPLink at the micro:bit VID/PID so it works
+// out of the box.
+const defaultFilters = [
+    { vendorId: 0x0d28, productId: 0x0204 }, // micro:bit DAPLink + Calliope mini 3
+    { vendorId: 0x1366, productId: 0x1015 }, // Segger J-Link — Calliope mini 2.0
+    { vendorId: 0x1366, productId: 0x1025 }, // Segger J-Link — Calliope mini 2.1
+];
 exports.DeviceSelectionMode = {
     /**
      * Attempts to connect to known device, otherwise asks which device to
@@ -351,6 +360,7 @@ class MicrobitUSBConnectionImpl extends events_js_1.TypedEventTarget {
         const data = await dataSource(boardVersion);
         const flashing = new partial_flashing_js_1.PartialFlashing(this.device, this.logging, boardVersion, pageSize, numPages);
         let wasPartial = false;
+        let flashSucceeded = false;
         try {
             if (partial) {
                 wasPartial = await flashing.flashAsync(data, progress);
@@ -358,6 +368,7 @@ class MicrobitUSBConnectionImpl extends events_js_1.TypedEventTarget {
             else {
                 await flashing.fullFlashAsync(data, progress);
             }
+            flashSucceeded = true;
         }
         finally {
             progress(wasPartial ? device_js_1.ProgressStage.PartialFlashing : device_js_1.ProgressStage.FullFlashing, undefined);
@@ -367,14 +378,29 @@ class MicrobitUSBConnectionImpl extends events_js_1.TypedEventTarget {
                 await this.disconnect(false, device_js_1.ConnectionStatus.Paused);
             }
             else {
-                await this.device.adi.reinit();
-                // Start serial before resetting so we capture startup output.
-                // For full flash FLASH_CLOSE already reset the target, so its
-                // early output accumulates in DAPLink's 512-byte serial ring
-                // buffer until the first read here.
-                if (this.hasSerialEventListeners()) {
-                    this.log("Reinstating serial after flash");
-                    await this.startSerialInternal();
+                // Patched by @calliope-edu/mini-connection-widget: the
+                // post-partial-flash reset MUST run even when reinit /
+                // serial reinstatement throw — previously any throw here
+                // skipped it, leaving a fully-flashed board halted at the
+                // flash stub's bkpt (dark display) while callers reported
+                // success. Reset failures are logged and rethrown (not
+                // silently swallowed) so callers can run their post-flash
+                // recovery instead of declaring an unstarted program done.
+                let postFlashError;
+                try {
+                    await this.device.adi.reinit();
+                    // Start serial before resetting so we capture startup output.
+                    // For full flash FLASH_CLOSE already reset the target, so its
+                    // early output accumulates in DAPLink's 512-byte serial ring
+                    // buffer until the first read here.
+                    if (this.hasSerialEventListeners()) {
+                        this.log("Reinstating serial after flash");
+                        await this.startSerialInternal();
+                    }
+                }
+                catch (e) {
+                    this.log("Post-flash reinit/serial failed: " + e);
+                    postFlashError = e;
                 }
                 if (wasPartial) {
                     // Partial flash writes pages via SWD without resetting.
@@ -384,9 +410,16 @@ class MicrobitUSBConnectionImpl extends events_js_1.TypedEventTarget {
                         await this.device.cortexM.reset();
                     }
                     catch (e) {
-                        // Allow errors on resetting, user can always manually reset if necessary.
+                        this.log("Post-flash reset failed: " + e);
+                        postFlashError = postFlashError ?? e;
                     }
                 }
+                // Only surface cleanup errors when the flash itself
+                // succeeded — never mask an in-flight flash error with a
+                // secondary cleanup failure.
+                if (postFlashError && flashSucceeded) {
+                    throw postFlashError;
+                }
             }
         }
     }
diff --git a/build/cjs/usb/daplink.js b/build/cjs/usb/daplink.js
index 39d7da6871f850ac8eedd516a381740ebff55550..32ca22bf5daa921e19d118ab9a317acf66fb0a7a 100644
--- a/build/cjs/usb/daplink.js
+++ b/build/cjs/usb/daplink.js
@@ -89,10 +89,23 @@ class DapLinkSerial {
         if (length === 0)
             return undefined;
         const bytes = new Uint8Array(result.buffer, 2, length);
-        return new TextDecoder().decode(bytes);
+        // Patched by @calliope-edu/mini-connection-widget: decode as
+        // ISO-8859-1 (Latin-1) so every byte 0..255 maps one-for-one to a
+        // code unit. UTF-8 decoding (the upstream default) replaces 0xFF
+        // and other byte ≥ 0x80 with the replacement char U+FFFD, which
+        // destroys the SFD in incoming Blocks frames and makes the
+        // BlocksFrameParser blind. Callers that want printable text can
+        // still read via String.fromCharCode roundtrip — only display of
+        // non-ASCII UTF-8 firmware logs changes (rare).
+        return new TextDecoder("iso-8859-1").decode(bytes);
     }
     async write(data) {
-        const encoded = new TextEncoder().encode(data);
+        // Patched by @calliope-edu/mini-connection-widget: accept either a
+        // string (UTF-8 encoded for legacy callers that pass text) or a
+        // Uint8Array (raw bytes, used for the Blocks wire protocol where
+        // 0xFF SFD and other high bytes MUST survive the trip without UTF-8
+        // mangling them into multi-byte sequences).
+        const encoded = data instanceof Uint8Array ? data : new TextEncoder().encode(data);
         const payload = new Uint8Array(encoded.length + 1);
         payload[0] = encoded.length;
         payload.set(encoded, 1);
@@ -165,12 +178,16 @@ async function dapLinkFlash(adi, buffer, onProgress) {
             if (writeResult.getUint8(1) === DAPLINK_ERROR_SUCCESS_DONE)
                 break;
         }
-        onProgress?.(1.0);
         const closeResult = await dap.send(exports.DAPLINK_VENDOR_FLASH_CLOSE);
         if (closeResult.getUint8(1) !== DAPLINK_ERROR_SUCCESS) {
             throw new cmsis_dap_js_1.DapError(`Flash close error (status=${closeResult.getUint8(1)})`);
         }
         await dap.send(exports.DAPLINK_VENDOR_FLASH_RESET);
+        // Patched by @calliope-edu/mini-connection-widget: report the final
+        // 1.0 only AFTER FLASH_CLOSE (which flushes the last stream data) and
+        // FLASH_RESET succeed, so callers can treat an exact 1.0 as "flash
+        // really complete" — mirroring the partial path's verified-then-1.0.
+        onProgress?.(1.0);
     }
     catch (error) {
         try {
diff --git a/build/cjs/usb/partial-flashing.js b/build/cjs/usb/partial-flashing.js
index 52ac6009ae741d29290dfd866626014bf43df0a4..cf9b60a35e3cc0ae0794d83001f6b52143ff9f48 100644
--- a/build/cjs/usb/partial-flashing.js
+++ b/build/cjs/usb/partial-flashing.js
@@ -190,7 +190,38 @@ class PartialFlashing {
             updateProgress(device_js_1.ProgressStage.PartialFlashing, i / pages.length);
             await this.partialFlashPageAsync(pages[i], pages[i + 1], i);
         }
-        updateProgress(device_js_1.ProgressStage.PartialFlashing, 1);
+        // Patched by @calliope-edu/mini-connection-widget: the final
+        // progress===1 is now emitted by partialFlashAsync only after the
+        // written pages have been checksum-verified (and UICR ensured), so
+        // callers can trust an exact 1.0 to mean "transfer really complete".
+    }
+    /**
+     * Patched by @calliope-edu/mini-connection-widget: write the changed
+     * pages, then re-run the on-chip checksum diff and re-flash any page
+     * that did not stick. Observed in the field: interleaved DAP traffic /
+     * WAIT-aborted transfers left pages silently missing or corrupted while
+     * the flash reported success ("dark display, shrinking changed-pages
+     * across attempts"). Bounded retries, then throw so callers fall back
+     * to a full flash instead of reporting an unverified partial flash.
+     */
+    async partialFlashVerifiedAsync(allPages, changed, updateProgress) {
+        let pending = changed;
+        for (let round = 0; round <= 2; round++) {
+            if (round > 0) {
+                this.log(`Verify: ${pending.length} page(s) still changed after write - re-flashing (round ${round})`);
+            }
+            // Retry rounds re-flash a handful of pages in well under a second;
+            // reporting their i/pending progress would visibly rewind the bar
+            // (fresh 0..1 over a shrunken denominator), so keep the UI quiet.
+            await this.partialFlashCoreAsync(pending, round === 0 ? updateProgress : () => { });
+            const checksums = await this.getFlashChecksumsAsync();
+            pending = (0, partial_flashing_utils_js_1.onlyChanged)(allPages, checksums, this.pageSize);
+            if (pending.length === 0) {
+                this.log("Verify: all pages match");
+                return;
+            }
+        }
+        throw new Error(`Partial flash verification failed: ${pending.length} page(s) still mismatched after retries`);
     }
     // Flash the micro:bit's ROM with the provided image by only copying over the pages that differ.
     // Falls back to a full flash if partial flashing fails.
@@ -199,10 +230,10 @@ class PartialFlashing {
         const memoryMap = this.toMemoryMap(data);
         const { flashBytes, uicrEntries } = this.extractFlashAndUicr(memoryMap);
         const checksums = await this.getFlashChecksumsAsync();
-        let aligned = (0, partial_flashing_utils_js_1.pageAlignBlocks)(flashBytes, 0, this.pageSize);
-        const totalPages = aligned.length;
+        const allPages = (0, partial_flashing_utils_js_1.pageAlignBlocks)(flashBytes, 0, this.pageSize);
+        const totalPages = allPages.length;
         this.log("Total pages: " + totalPages);
-        aligned = (0, partial_flashing_utils_js_1.onlyChanged)(aligned, checksums, this.pageSize);
+        const aligned = (0, partial_flashing_utils_js_1.onlyChanged)(allPages, checksums, this.pageSize);
         this.log("Changed pages: " + aligned.length);
         let partial;
         if (aligned.length > totalPages / 2) {
@@ -217,13 +248,13 @@ class PartialFlashing {
                 // so we must reconnect before partial flash can use it.
                 await this.device.adi.reinit();
                 await this.device.cortexM.reset(true);
-                await this.partialFlashCoreAsync(aligned, updateProgress);
+                await this.partialFlashVerifiedAsync(allPages, aligned, updateProgress);
                 partial = true;
             }
         }
         else {
             try {
-                await this.partialFlashCoreAsync(aligned, updateProgress);
+                await this.partialFlashVerifiedAsync(allPages, aligned, updateProgress);
                 partial = true;
             }
             catch (e) {
@@ -238,6 +269,11 @@ class PartialFlashing {
         if (partial && uicrEntries.length > 0) {
             partial = await this.ensureUicr(data, uicrEntries, updateProgress);
         }
+        // Patched: only now — pages verified + UICR ensured — report the
+        // exact 1.0 that callers treat as "byte transfer complete".
+        if (partial) {
+            updateProgress(device_js_1.ProgressStage.PartialFlashing, 1);
+        }
         this.log("Flashing complete");
         return partial;
     }
diff --git a/build/esm/bluetooth/connection.js b/build/esm/bluetooth/connection.js
index 6ee75313cf8ee01d1a986112c6d101289e95eef2..1828d0427707cbe6614b84512a12e97b006ff694 100644
--- a/build/esm/bluetooth/connection.js
+++ b/build/esm/bluetooth/connection.js
@@ -246,9 +246,12 @@ class MicrobitBluetoothConnectionImpl extends TypedEventTarget {
         // Support both the default "BBC micro:bit" name and the shorter "uBit"
         // name used by some MakeCode extensions (e.g. bsiever/microbit-pxt-blehid).
         // See https://github.com/bsiever/microbit-pxt-blehid/issues/31
+        // Patched by @calliope-edu/mini-connection-widget: include Calliope
+        // mini namePrefix so Calliope hardware shows up in the BLE picker
+        // alongside micro:bit. Mini 1/2/3 all advertise "Calliope mini [name]".
         const namePrefixes = this.nameFilter
-            ? [`BBC micro:bit [${this.nameFilter}]`, `uBit [${this.nameFilter}]`]
-            : ["BBC micro:bit", "uBit"];
+            ? [`BBC micro:bit [${this.nameFilter}]`, `uBit [${this.nameFilter}]`, `Calliope mini [${this.nameFilter}]`]
+            : ["BBC micro:bit", "uBit", "Calliope mini"];
         // If we have a cached device, check if it still matches the current filter.
         // If not, clear it so we find a new device.
         if (this.bleDevice) {
@@ -491,6 +494,11 @@ class MicrobitBluetoothConnectionImpl extends TypedEventTarget {
             profile.magnetometer.id,
             profile.temperature.id,
             profile.uart.id,
+            // Patched by @calliope-edu/mini-connection-widget: declare the
+            // partial-flashing service up front so flash() can reach it on
+            // Web Bluetooth. (Capacitor's web fallback can't reach undeclared
+            // services after pairing.)
+            profile.partialFlashing.id,
         ];
         // Temporarily patch navigator.bluetooth.requestDevice to support multiple
         // name prefix filters. The capacitor-ble plugin only supports a single
diff --git a/build/esm/bluetooth/services/device-information-service.js b/build/esm/bluetooth/services/device-information-service.js
index 496d40d498fbbe95895f48646c77f71c5f5a669c..31dbc2ad36849f54445416495cda6b4f62b12308 100644
--- a/build/esm/bluetooth/services/device-information-service.js
+++ b/build/esm/bluetooth/services/device-information-service.js
@@ -13,13 +13,21 @@ export class DeviceInformationService {
         const serviceMeta = profile.deviceInformation;
         const modelNumberBytes = await BleClient.read(this.deviceId, serviceMeta.id, serviceMeta.characteristics.modelNumber.id);
         const modelNumber = new TextDecoder().decode(modelNumberBytes);
-        if (modelNumber.toLowerCase() === "BBC micro:bit".toLowerCase()) {
+        const lower = modelNumber.toLowerCase();
+        if (lower === "BBC micro:bit".toLowerCase()) {
             return "V1";
         }
-        if (modelNumber.toLowerCase().includes("BBC micro:bit v2".toLowerCase())) {
+        // Patched by @calliope-edu/mini-connection-widget: recognise Calliope
+        // mini hardware (mini 1/2 → V1-class DAL, mini 3 → V2-class CODAL)
+        // and tolerate firmware-revision suffixes that aren't strict matches.
+        if (lower.includes("calliope mini") || lower.includes("v2")) {
             return "V2";
         }
-        throw new Error(`Unexpected model number: ${modelNumber}`);
+        // Unknown model number — log + default to V2 instead of throwing.
+        // The only consumer of board version on the BLE path picks between
+        // V1 and V2 capability sets; defaulting to V2 is the safe bet.
+        console.warn(`[microbit-connection] Unrecognised model number "${modelNumber}", assuming V2`);
+        return "V2";
     }
 }
 //# sourceMappingURL=device-information-service.js.map
\ No newline at end of file
diff --git a/build/esm/usb/connection.d.ts b/build/esm/usb/connection.d.ts
index 5d63b9ce50c3c80907ff33444c2898f118379e32..081a4e871a7e056b6d3e75760e5f4585ae7ab553 100644
--- a/build/esm/usb/connection.d.ts
+++ b/build/esm/usb/connection.d.ts
@@ -58,7 +58,7 @@ export interface MicrobitUSBConnection extends DeviceConnection {
      * @returns A promise that resolves when the write is complete.
      * @throws {DeviceError} with code `not-connected` if there is no connection.
      */
-    serialWrite(data: string): Promise<void>;
+    serialWrite(data: string | Uint8Array): Promise<void>;
     /**
      * Gets micro:bit deviceId.
      *
diff --git a/build/esm/usb/connection.js b/build/esm/usb/connection.js
index f685da48a3bcab4dfcd4a13667efc2ff902dfb51..0559cdf94cba79c794b1930dd2006bb1ad5bd641 100644
--- a/build/esm/usb/connection.js
+++ b/build/esm/usb/connection.js
@@ -12,7 +12,16 @@ import { PromiseQueue } from "./promise-queue.js";
 import { USBDeviceWrapper, } from "./device-wrapper.js";
 import { PartialFlashing } from "./partial-flashing.js";
 import { saturateCdcPipeline } from "./cdc-saturation.js";
-const defaultFilters = [{ vendorId: 0x0d28, productId: 0x0204 }];
+// Patched by @calliope-edu/mini-connection-widget: include Segger J-Link
+// VID/PID pairs used by Calliope mini 1.x and 2.x boards. Without these,
+// Calliope minis using J-Link debugger firmware are invisible to the
+// WebUSB picker. Mini 3 ships DAPLink at the micro:bit VID/PID so it works
+// out of the box.
+const defaultFilters = [
+    { vendorId: 0x0d28, productId: 0x0204 }, // micro:bit DAPLink + Calliope mini 3
+    { vendorId: 0x1366, productId: 0x1015 }, // Segger J-Link — Calliope mini 2.0
+    { vendorId: 0x1366, productId: 0x1025 }, // Segger J-Link — Calliope mini 2.1
+];
 export const DeviceSelectionMode = {
     /**
      * Attempts to connect to known device, otherwise asks which device to
@@ -347,6 +356,7 @@ class MicrobitUSBConnectionImpl extends TypedEventTarget {
         const data = await dataSource(boardVersion);
         const flashing = new PartialFlashing(this.device, this.logging, boardVersion, pageSize, numPages);
         let wasPartial = false;
+        let flashSucceeded = false;
         try {
             if (partial) {
                 wasPartial = await flashing.flashAsync(data, progress);
@@ -354,6 +364,7 @@ class MicrobitUSBConnectionImpl extends TypedEventTarget {
             else {
                 await flashing.fullFlashAsync(data, progress);
             }
+            flashSucceeded = true;
         }
         finally {
             progress(wasPartial ? ProgressStage.PartialFlashing : ProgressStage.FullFlashing, undefined);
@@ -363,14 +374,29 @@ class MicrobitUSBConnectionImpl extends TypedEventTarget {
                 await this.disconnect(false, ConnectionStatus.Paused);
             }
             else {
-                await this.device.adi.reinit();
-                // Start serial before resetting so we capture startup output.
-                // For full flash FLASH_CLOSE already reset the target, so its
-                // early output accumulates in DAPLink's 512-byte serial ring
-                // buffer until the first read here.
-                if (this.hasSerialEventListeners()) {
-                    this.log("Reinstating serial after flash");
-                    await this.startSerialInternal();
+                // Patched by @calliope-edu/mini-connection-widget: the
+                // post-partial-flash reset MUST run even when reinit /
+                // serial reinstatement throw — previously any throw here
+                // skipped it, leaving a fully-flashed board halted at the
+                // flash stub's bkpt (dark display) while callers reported
+                // success. Reset failures are logged and rethrown (not
+                // silently swallowed) so callers can run their post-flash
+                // recovery instead of declaring an unstarted program done.
+                let postFlashError;
+                try {
+                    await this.device.adi.reinit();
+                    // Start serial before resetting so we capture startup output.
+                    // For full flash FLASH_CLOSE already reset the target, so its
+                    // early output accumulates in DAPLink's 512-byte serial ring
+                    // buffer until the first read here.
+                    if (this.hasSerialEventListeners()) {
+                        this.log("Reinstating serial after flash");
+                        await this.startSerialInternal();
+                    }
+                }
+                catch (e) {
+                    this.log("Post-flash reinit/serial failed: " + e);
+                    postFlashError = e;
                 }
                 if (wasPartial) {
                     // Partial flash writes pages via SWD without resetting.
@@ -380,9 +406,16 @@ class MicrobitUSBConnectionImpl extends TypedEventTarget {
                         await this.device.cortexM.reset();
                     }
                     catch (e) {
-                        // Allow errors on resetting, user can always manually reset if necessary.
+                        this.log("Post-flash reset failed: " + e);
+                        postFlashError = postFlashError ?? e;
                     }
                 }
+                // Only surface cleanup errors when the flash itself
+                // succeeded — never mask an in-flight flash error with a
+                // secondary cleanup failure.
+                if (postFlashError && flashSucceeded) {
+                    throw postFlashError;
+                }
             }
         }
     }
diff --git a/build/esm/usb/daplink.js b/build/esm/usb/daplink.js
index 50c5a21add13678ef49e1857b380bfaad2b0e82d..a62de8b9ba71964b6ce840299a9e32deeb3edd1f 100644
--- a/build/esm/usb/daplink.js
+++ b/build/esm/usb/daplink.js
@@ -83,10 +83,23 @@ export class DapLinkSerial {
         if (length === 0)
             return undefined;
         const bytes = new Uint8Array(result.buffer, 2, length);
-        return new TextDecoder().decode(bytes);
+        // Patched by @calliope-edu/mini-connection-widget: decode as
+        // ISO-8859-1 (Latin-1) so every byte 0..255 maps one-for-one to a
+        // code unit. UTF-8 decoding (the upstream default) replaces 0xFF
+        // and other byte ≥ 0x80 with the replacement char U+FFFD, which
+        // destroys the SFD in incoming Blocks frames and makes the
+        // BlocksFrameParser blind. Callers that want printable text can
+        // still read via String.fromCharCode roundtrip — only display of
+        // non-ASCII UTF-8 firmware logs changes (rare).
+        return new TextDecoder("iso-8859-1").decode(bytes);
     }
     async write(data) {
-        const encoded = new TextEncoder().encode(data);
+        // Patched by @calliope-edu/mini-connection-widget: accept either a
+        // string (UTF-8 encoded for legacy callers that pass text) or a
+        // Uint8Array (raw bytes, used for the Blocks wire protocol where
+        // 0xFF SFD and other high bytes MUST survive the trip without UTF-8
+        // mangling them into multi-byte sequences).
+        const encoded = data instanceof Uint8Array ? data : new TextEncoder().encode(data);
         const payload = new Uint8Array(encoded.length + 1);
         payload[0] = encoded.length;
         payload.set(encoded, 1);
@@ -158,12 +171,16 @@ export async function dapLinkFlash(adi, buffer, onProgress) {
             if (writeResult.getUint8(1) === DAPLINK_ERROR_SUCCESS_DONE)
                 break;
         }
-        onProgress?.(1.0);
         const closeResult = await dap.send(DAPLINK_VENDOR_FLASH_CLOSE);
         if (closeResult.getUint8(1) !== DAPLINK_ERROR_SUCCESS) {
             throw new DapError(`Flash close error (status=${closeResult.getUint8(1)})`);
         }
         await dap.send(DAPLINK_VENDOR_FLASH_RESET);
+        // Patched by @calliope-edu/mini-connection-widget: report the final
+        // 1.0 only AFTER FLASH_CLOSE (which flushes the last stream data) and
+        // FLASH_RESET succeed, so callers can treat an exact 1.0 as "flash
+        // really complete" — mirroring the partial path's verified-then-1.0.
+        onProgress?.(1.0);
     }
     catch (error) {
         try {
diff --git a/build/esm/usb/partial-flashing.js b/build/esm/usb/partial-flashing.js
index 29760394b36be2cac5a80812f22346da97c04878..2d31048f3ba07b164d2d6a7bd263e2b3a52a495f 100644
--- a/build/esm/usb/partial-flashing.js
+++ b/build/esm/usb/partial-flashing.js
@@ -184,7 +184,38 @@ export class PartialFlashing {
             updateProgress(ProgressStage.PartialFlashing, i / pages.length);
             await this.partialFlashPageAsync(pages[i], pages[i + 1], i);
         }
-        updateProgress(ProgressStage.PartialFlashing, 1);
+        // Patched by @calliope-edu/mini-connection-widget: the final
+        // progress===1 is now emitted by partialFlashAsync only after the
+        // written pages have been checksum-verified (and UICR ensured), so
+        // callers can trust an exact 1.0 to mean "transfer really complete".
+    }
+    /**
+     * Patched by @calliope-edu/mini-connection-widget: write the changed
+     * pages, then re-run the on-chip checksum diff and re-flash any page
+     * that did not stick. Observed in the field: interleaved DAP traffic /
+     * WAIT-aborted transfers left pages silently missing or corrupted while
+     * the flash reported success ("dark display, shrinking changed-pages
+     * across attempts"). Bounded retries, then throw so callers fall back
+     * to a full flash instead of reporting an unverified partial flash.
+     */
+    async partialFlashVerifiedAsync(allPages, changed, updateProgress) {
+        let pending = changed;
+        for (let round = 0; round <= 2; round++) {
+            if (round > 0) {
+                this.log(`Verify: ${pending.length} page(s) still changed after write - re-flashing (round ${round})`);
+            }
+            // Retry rounds re-flash a handful of pages in well under a second;
+            // reporting their i/pending progress would visibly rewind the bar
+            // (fresh 0..1 over a shrunken denominator), so keep the UI quiet.
+            await this.partialFlashCoreAsync(pending, round === 0 ? updateProgress : () => { });
+            const checksums = await this.getFlashChecksumsAsync();
+            pending = onlyChanged(allPages, checksums, this.pageSize);
+            if (pending.length === 0) {
+                this.log("Verify: all pages match");
+                return;
+            }
+        }
+        throw new Error(`Partial flash verification failed: ${pending.length} page(s) still mismatched after retries`);
     }
     // Flash the micro:bit's ROM with the provided image by only copying over the pages that differ.
     // Falls back to a full flash if partial flashing fails.
@@ -193,10 +224,10 @@ export class PartialFlashing {
         const memoryMap = this.toMemoryMap(data);
         const { flashBytes, uicrEntries } = this.extractFlashAndUicr(memoryMap);
         const checksums = await this.getFlashChecksumsAsync();
-        let aligned = pageAlignBlocks(flashBytes, 0, this.pageSize);
-        const totalPages = aligned.length;
+        const allPages = pageAlignBlocks(flashBytes, 0, this.pageSize);
+        const totalPages = allPages.length;
         this.log("Total pages: " + totalPages);
-        aligned = onlyChanged(aligned, checksums, this.pageSize);
+        const aligned = onlyChanged(allPages, checksums, this.pageSize);
         this.log("Changed pages: " + aligned.length);
         let partial;
         if (aligned.length > totalPages / 2) {
@@ -211,13 +242,13 @@ export class PartialFlashing {
                 // so we must reconnect before partial flash can use it.
                 await this.device.adi.reinit();
                 await this.device.cortexM.reset(true);
-                await this.partialFlashCoreAsync(aligned, updateProgress);
+                await this.partialFlashVerifiedAsync(allPages, aligned, updateProgress);
                 partial = true;
             }
         }
         else {
             try {
-                await this.partialFlashCoreAsync(aligned, updateProgress);
+                await this.partialFlashVerifiedAsync(allPages, aligned, updateProgress);
                 partial = true;
             }
             catch (e) {
@@ -232,6 +263,11 @@ export class PartialFlashing {
         if (partial && uicrEntries.length > 0) {
             partial = await this.ensureUicr(data, uicrEntries, updateProgress);
         }
+        // Patched: only now — pages verified + UICR ensured — report the
+        // exact 1.0 that callers treat as "byte transfer complete".
+        if (partial) {
+            updateProgress(ProgressStage.PartialFlashing, 1);
+        }
         this.log("Flashing complete");
         return partial;
     }
